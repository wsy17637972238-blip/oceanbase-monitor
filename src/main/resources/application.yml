server:
  port: 8080

spring:
  datasource:
    # OceanBase MySQL 租户（test），通过 MySQL 协议连接
    url: jdbc:mysql://127.0.0.1:2881/obinspection_check?characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root@test
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
